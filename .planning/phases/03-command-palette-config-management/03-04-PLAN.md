---
phase: 03-command-palette-config-management
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified: []
autonomous: false
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CONF-02, CONF-03, CONF-05, NAV-06]

must_haves:
  truths:
    - "Cmd+K opens command palette and fuzzy search works"
    - "Add app from palette creates new sidebar entry"
    - "Remove app from palette or context menu removes it"
    - "Drag and drop reorder persists to apps.json"
    - "All four Phase 3 success criteria pass"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 3 success criteria with human testing.

Purpose: Command palette, DnD reorder, context menus, and menu bar all require interactive verification that cannot be automated — keyboard shortcuts, visual drag feedback, native menus.
Output: Confirmation that all 4 success criteria pass or list of issues to fix.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-command-palette-config-management/03-CONTEXT.md
@.planning/phases/03-command-palette-config-management/03-01-SUMMARY.md
@.planning/phases/03-command-palette-config-management/03-02-SUMMARY.md
@.planning/phases/03-command-palette-config-management/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated tests</name>
  <files></files>
  <action>
    Run the full test suite to confirm everything passes before human verification:
    1. `cargo test -p nexus` — Rust tests
    2. `npx vitest run` — Frontend tests (configMutations etc.)
    3. `npx tsc --noEmit` — TypeScript type checking
    If any fail, fix before proceeding to human verification.
  </action>
  <verify>
    <automated>cd /Users/angelbotto/Dev/hacks/nexus && cargo build -p nexus 2>&1 | tail -3 && npx vitest run 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>All automated checks pass: Rust compiles, Vitest green, TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 3 success criteria</name>
  <action>
    Human verifies all four Phase 3 success criteria interactively. Start the app with `cargo tauri dev`.
  </action>
  <verify>Human confirms all criteria pass or reports issues.</verify>
  <done>All four Phase 3 success criteria verified by human.</done>
  <what-built>
    Complete Phase 3: Command palette with fuzzy search and actions, sidebar drag-and-drop reorder, native macOS context menus, and native menu bar.
  </what-built>
  <how-to-verify>
    Start the app: `cd /Users/angelbotto/Dev/hacks/nexus && cargo tauri dev`

    **Success Criteria 1 — Command Palette (Cmd+K):**
    1. Press Cmd+K — palette overlay should appear (centered top, dimmed background)
    2. Type part of an app name — fuzzy search filters the list
    3. Press Enter on a result — switches to that app, palette closes
    4. Press Cmd+K again, type ">" — action mode shows Add/Remove/Reload/Toggle
    5. Press Escape — palette closes

    **Success Criteria 2 — Add App:**
    1. Press Cmd+K, type "> Add" and select "Add new app"
    2. Enter a URL (e.g. https://github.com) and name (e.g. "GitHub")
    3. Submit — new app appears in sidebar immediately
    4. Check `~/.nexus/apps.json` — new entry is there
    5. Alternatively: press Cmd+N — should open palette directly in add-form mode

    **Success Criteria 3 — Remove App:**
    1. Right-click an app in sidebar — native macOS context menu appears
    2. Click "Remove" — app disappears from sidebar
    3. Check `~/.nexus/apps.json` — entry is gone
    4. If the removed app was active, an empty state should show
    5. Also test: Cmd+K > "> Remove current app" with an active app

    **Success Criteria 4 — Drag & Drop Reorder:**
    1. Click and drag an app in the sidebar to a new position
    2. Visual feedback: dragged item at 50% opacity, insertion line between items
    3. Release — app is in new position
    4. Check `~/.nexus/apps.json` — order is updated
    5. Try dragging an app to a different group

    **Bonus checks:**
    - Menu bar shows Nexus/File/View with correct shortcuts documented
    - Context menu shows Open/Reload/Edit.../Remove with native macOS styling
    - "Edit..." from context menu opens palette with pre-filled name/URL
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
All four Phase 3 success criteria verified by human testing.
</verification>

<success_criteria>
- All automated tests pass
- Human confirms all 4 success criteria from ROADMAP
- No regressions in Phase 1/2 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-command-palette-config-management/03-04-SUMMARY.md`
</output>
