---
phase: 05-cross-platform-distribution
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified: []
autonomous: false
requirements:
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PERF-04

must_haves:
  truths:
    - "macOS build runs and all features work (sidebar, shortcuts, switching, badges)"
    - "CI workflow file is syntactically valid and references correct matrix platforms"
    - "All Rust tests and TypeScript compilation pass"
    - "Binary size on macOS release build is under 15 MB"
  artifacts: []
  key_links: []
---

<objective>
Verify Phase 5 success criteria: macOS runs correctly with all cross-platform changes, CI workflow is ready, and binary size target is met.

Purpose: Final gate before shipping v1. Confirms that platform-specific code changes did not regress macOS functionality, and that the CI pipeline is correctly configured for all platforms.

Output: Human-verified confirmation that Phase 5 is complete.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cross-platform-distribution/05-CONTEXT.md
@.planning/phases/05-cross-platform-distribution/05-01-SUMMARY.md
@.planning/phases/05-cross-platform-distribution/05-02-SUMMARY.md
@.planning/phases/05-cross-platform-distribution/05-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 5 cross-platform readiness</name>
  <files></files>
  <action>
Run the full automated verification suite, then present results to the human for manual verification.

**Automated checks (Claude runs these):**
1. `cargo test --manifest-path src-tauri/Cargo.toml` — all Rust tests pass
2. `npx tsc --noEmit` — TypeScript compiles
3. `npm test` — Vitest tests pass
4. `cargo build --release --manifest-path src-tauri/Cargo.toml` — release build succeeds
5. `ls -lh src-tauri/target/release/nexus` — verify binary is under 15 MB
6. `grep -r "Modifiers::SUPER" src-tauri/src/` — should return no matches
7. `grep -A1 "target_os.*macos" src-tauri/Cargo.toml | grep objc2` — objc2 behind cfg guard
8. `ls src-tauri/tauri.*.conf.json` — platform config overrides exist
9. `cat .github/workflows/publish.yml | head -10` — CI workflow exists

Report all results, then ask the human to:
1. Run `npm run tauri dev` and verify the app launches normally on macOS
2. Test sidebar navigation, app switching (Cmd+1-9), Cmd+B toggle, Cmd+K palette
3. Verify no UpdateBanner shows (expected — no update manifest in dev mode)
4. Review `.github/workflows/publish.yml` — confirm it targets correct platforms and triggers on tags only
5. Confirm they have (or will generate) the signing key pair for GitHub Secrets

**To complete CI setup after verification:**
```
npm run tauri signer generate -- -w ~/.tauri/nexus.key
# Copy ~/.tauri/nexus.key content -> GitHub Secret: TAURI_SIGNING_PRIVATE_KEY
# Copy ~/.tauri/nexus.key.pub content -> replace REPLACE_WITH_PUBLIC_KEY in src-tauri/tauri.conf.json
# Add password -> GitHub Secret: TAURI_SIGNING_PRIVATE_KEY_PASSWORD
```

**To trigger first build:**
```
git tag v0.1.0
git push --tags
```
  </action>
  <verify>Human confirms macOS runs correctly and CI setup looks good. Type "approved" or describe issues.</verify>
  <done>Human approved: macOS verified working with all cross-platform changes, CI workflow reviewed, binary size confirmed under 15 MB. Phase 5 complete.</done>
</task>

</tasks>

<verification>
- macOS app launches and all features work with cross-platform code changes
- Binary size under 15 MB (PERF-04)
- CI workflow references all 3 platforms (PLAT-02, PLAT-03, PLAT-04)
- No macOS-only code leaks outside cfg guards
- UpdateBanner renders without errors (even if no update available)
</verification>

<success_criteria>
Phase 5 complete: Nexus is ready for cross-platform distribution. macOS verified working, CI pipeline configured for Linux and Windows builds, auto-updater integrated, binary size optimized. User can ship v1 by generating a signing key and pushing a version tag.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-platform-distribution/05-04-SUMMARY.md`
</output>
