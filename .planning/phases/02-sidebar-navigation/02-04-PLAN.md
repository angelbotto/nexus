---
phase: 02-sidebar-navigation
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, KEY-01, KEY-02, KEY-03, WEB-08, VIS-01, VIS-02, VIS-04]

must_haves:
  truths:
    - "All 5 Phase 2 success criteria are met"
    - "User can visually confirm Arc-inspired dark aesthetic"
    - "All keyboard shortcuts work with webview focused"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 2 success criteria with a human smoke test. All automation is complete from Plans 01-03; this checkpoint confirms visual and functional correctness.

Purpose: Keyboard shortcuts, visual aesthetics, and webview behavior require runtime verification that automated tests cannot provide.
Output: Human sign-off that Phase 2 is complete.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-sidebar-navigation/02-CONTEXT.md
@.planning/phases/02-sidebar-navigation/02-01-SUMMARY.md
@.planning/phases/02-sidebar-navigation/02-02-SUMMARY.md
@.planning/phases/02-sidebar-navigation/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 2: Full smoke test of sidebar, shortcuts, and Arc aesthetic</name>
  <files>none</files>
  <action>
Build and launch the app for human verification. Run `pnpm tauri dev` in the project root. Present the verification checklist below to the user and wait for approval.

This is a human-verify checkpoint — all implementation is complete from Plans 01-03. The executor should launch the app and guide the user through the checklist.
  </action>
  <verify>Human approval of all 5 success criteria</verify>
  <done>User types "approved" confirming all criteria pass, or describes issues to fix</done>
  <what-built>
Complete sidebar navigation with grouped apps, collapsible sections, Arc-inspired dark mode, keyboard shortcuts (Cmd+1-9, Cmd+B, Cmd+R), webview card layout, and startup restore.
  </what-built>
  <how-to-verify>
Build and launch the app:
```bash
cd /Users/angelbotto/Dev/hacks/nexus && pnpm tauri dev
```

**Success Criterion 1 — Click to switch with active highlight:**
1. Click any app in the sidebar
2. Verify the clicked app's webview loads
3. Verify the active app has a subtle white highlight (bg-white/10) and white text
4. Click a different app — previous one deactivates, new one highlights

**Success Criterion 2 — Cmd+1-9 shortcuts:**
1. Click inside an app webview (so it has keyboard focus)
2. Press Cmd+1 — should jump to the first app
3. Press Cmd+2 — should jump to the second app
4. Verify shortcuts work even with webview focused (not just sidebar)

**Success Criterion 3 — Cmd+B sidebar toggle:**
1. Press Cmd+B — sidebar should disappear instantly (no animation)
2. Verify webview fills the full window width
3. Press Cmd+B again — sidebar reappears
4. Quit and relaunch — sidebar state should persist

**Success Criterion 4 — Cmd+R reload:**
1. Open an app (e.g., GitHub)
2. Press Cmd+R — page should silently reload (no visual feedback, just reload)

**Success Criterion 5 — Grouped sidebar with collapse:**
1. Verify apps are grouped under headers (e.g., "MIS PRODUCTOS", "TOOLS")
2. Click a group header — apps in that group should hide (chevron rotates)
3. Click again — apps reappear
4. Quit and relaunch — collapsed state persists

**Visual checks (Arc aesthetic):**
- Sidebar is very dark (near-black, #111117-ish)
- Main background slightly lighter (gray-950)
- Webview appears as a rounded card with a visible gap around it
- No separator line between sidebar and content
- No "Nexus" title in sidebar
- Monochromatic: no accent colors, only white/gray tones

**Startup restore:**
1. Switch to a specific app
2. Quit and relaunch
3. The same app should auto-load on startup
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
All 5 ROADMAP success criteria pass human verification.
</verification>

<success_criteria>
- Human confirms all 5 success criteria
- Visual aesthetic matches Arc-inspired dark mode decisions
- No regressions from Phase 1 (session isolation, external links, config hot-reload)
</success_criteria>

<output>
After completion, create `.planning/phases/02-sidebar-navigation/02-04-SUMMARY.md`
</output>
