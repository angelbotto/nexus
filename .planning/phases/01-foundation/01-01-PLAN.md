---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/config.rs
  - src-tauri/src/state.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/config.rs
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src/main.tsx
  - src/App.tsx
  - src/types.ts
  - src/index.css
  - vite.config.ts
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - CONF-01
  - PLAT-01

must_haves:
  truths:
    - "App scaffolds and builds on macOS arm64 without errors"
    - "Config loads from ~/.nexus/apps.json with correct schema (id, name, url, group + groups section)"
    - "First run auto-creates ~/.nexus/apps.json with default apps (Plane, Linear, Gmail, GitHub)"
    - "Invalid/corrupt JSON falls back to default config without crashing"
  artifacts:
    - path: "src-tauri/src/config.rs"
      provides: "NexusConfig, AppConfig, GroupConfig structs + load_or_create_config + default_config"
      contains: "load_or_create_config"
    - path: "src-tauri/src/state.rs"
      provides: "AppState struct with config and webview tracking"
      contains: "AppState"
    - path: "src-tauri/src/commands/config.rs"
      provides: "load_config IPC command"
      contains: "tauri::command"
    - path: "src/types.ts"
      provides: "TypeScript types mirroring Rust config structs"
      contains: "AppConfig"
    - path: "src/App.tsx"
      provides: "Root React component rendering"
      contains: "function App"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/config.rs"
      via: "config::load_or_create_config() in setup closure"
      pattern: "load_or_create_config"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/config.rs"
      via: "generate_handler! macro"
      pattern: "generate_handler"
---

<objective>
Scaffold the Tauri 2 project and build the config layer with first-run support.

Purpose: Establish the project foundation -- Tauri scaffold with React + TypeScript + Vite + Tailwind CSS v4, Rust config module that reads/writes ~/.nexus/apps.json, AppState managed state, and the load_config IPC command. This is the base everything else builds on.

Output: A buildable Tauri 2 app that loads config from disk and exposes it to the React frontend via IPC.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Tauri 2 project and build config layer with tests</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/src/main.rs,
    src-tauri/src/lib.rs,
    src-tauri/src/config.rs,
    src-tauri/src/state.rs,
    src-tauri/src/commands/mod.rs,
    src-tauri/src/commands/config.rs,
    src-tauri/tauri.conf.json,
    src-tauri/capabilities/default.json,
    package.json,
    vite.config.ts,
    tsconfig.json,
    src/main.tsx,
    src/App.tsx,
    src/types.ts,
    src/index.css
  </files>
  <behavior>
    - Test: NexusConfig JSON round-trip (serialize then deserialize produces identical struct)
    - Test: load_or_create_config creates default file when path does not exist
    - Test: load_or_create_config reads existing valid JSON file correctly
    - Test: load_or_create_config falls back to default when JSON is invalid/corrupt
    - Test: default_config contains exactly 4 apps (plane, linear, gmail, github) in 2 groups
    - Test: plane.botto.is is in the "mis-productos" group
    - Test: apps with invalid group id get handled (config still loads)
  </behavior>
  <action>
    1. Scaffold with `npm create tauri-app@latest` selecting React + TypeScript template, OR manually create the project structure if the scaffold CLI is interactive. The result must match the structure from RESEARCH.md.

    2. Configure Tailwind CSS v4:
       - Install `@tailwindcss/vite` as dev dependency
       - Add `tailwindcss()` plugin to `vite.config.ts`
       - Set `src/index.css` to `@import "tailwindcss";`
       - No `tailwind.config.js` needed (v4 style)

    3. Add Rust dependencies to `src-tauri/Cargo.toml`:
       - `tauri-plugin-fs = "2"` (for file watching in Plan 02)
       - `tauri-plugin-opener = "2"` (for external links in Plan 03)
       - `dirs = "5"` (home dir resolution)
       - `uuid = { version = "1", features = ["v4"] }` (for data_store_identifier)
       - `serde = { version = "1", features = ["derive"] }` + `serde_json = "1"`
       - `md5 = "0.7"` (for deterministic 16-byte store IDs)

    4. Add JS dependencies:
       - `@tauri-apps/plugin-fs` and `@tauri-apps/plugin-opener`

    5. Create `src-tauri/src/config.rs`:
       - `AppConfig` struct: `id: String, name: String, url: String, group: String` (all derive Serialize, Deserialize, Clone, Debug, PartialEq)
       - `GroupConfig` struct: `id: String, name: String`
       - `NexusConfig` struct: `groups: Vec<GroupConfig>, apps: Vec<AppConfig>`
       - `config_path() -> PathBuf` using `dirs::home_dir()` + `.nexus/apps.json`
       - `default_config() -> NexusConfig` with the 4 default apps per CONTEXT.md (plane.botto.is in "mis-productos")
       - `load_or_create_config() -> NexusConfig` -- loads from file, creates default if missing, falls back to default on parse error
       - `#[cfg(test)] mod tests` with the behavior tests listed above. Use `tempdir` or `tempfile` crate for isolated file tests (add as dev-dependency).

    6. Create `src-tauri/src/state.rs`:
       - `AppState` struct with `config: NexusConfig`, `active_app_id: Option<String>`, `webviews_created: HashSet<String>`
       - `AppState::new(config: NexusConfig) -> Self` constructor

    7. Create `src-tauri/src/commands/config.rs`:
       - `#[tauri::command] pub fn load_config(state: State<'_, Mutex<AppState>>) -> Result<NexusConfig, String>` -- returns current config from state

    8. Create `src-tauri/src/commands/mod.rs` re-exporting config module.

    9. Wire `src-tauri/src/lib.rs`:
       - Register `tauri_plugin_fs` and `tauri_plugin_opener` plugins
       - In `setup()`: call `load_or_create_config()`, manage `Mutex::new(AppState::new(config))`
       - Register `commands::config::load_config` in `generate_handler!`

    10. Configure `src-tauri/capabilities/default.json` with permissions: core:default, core:path:default, core:window:default, core:webview:default, fs:default, fs:allow-watch, opener:default, opener:allow-open-url.

    11. Set `tauri.conf.json`: app identifier "is.botto.nexus", window title "Nexus", set `"minimumSystemVersion": "14.0"` in macOS bundle config.

    12. Create `src/types.ts` mirroring Rust structs:
       - `interface AppConfig { id: string; name: string; url: string; group: string; }`
       - `interface GroupConfig { id: string; name: string; }`
       - `interface NexusConfig { groups: GroupConfig[]; apps: AppConfig[]; }`

    13. Create minimal `src/App.tsx` that calls `invoke('load_config')` on mount and renders the app list as plain text (just to verify IPC works). Basic Tailwind styling.

    CRITICAL per RESEARCH.md:
    - Use `data_store_identifier` NOT `data_directory` for macOS
    - All IPC through Rust (not from inside webviews)
    - Webviews NOT created at startup (lazy, on first click)
  </action>
  <verify>
    <automated>cd src-tauri && cargo test -- config 2>&1 | tail -20 && cd .. && cargo tauri build --debug 2>&1 | tail -5 || cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `cargo test -p nexus` passes all config tests (serde round-trip, first-run creation, corrupt fallback, default apps verification)
    - `cargo build` succeeds on macOS arm64
    - React frontend renders and can invoke `load_config` IPC command
    - ~/.nexus/apps.json is auto-created on first run with 4 default apps
  </done>
</task>

</tasks>

<verification>
- `cargo test -p nexus` -- all config unit tests pass
- `cargo build --manifest-path src-tauri/Cargo.toml` -- compiles without errors
- Project structure matches RESEARCH.md recommended layout
</verification>

<success_criteria>
- Tauri 2 project scaffolded with React 18 + TypeScript + Vite + Tailwind CSS v4
- Config module loads/creates ~/.nexus/apps.json with correct schema
- First-run experience works (default config auto-created)
- AppState managed in Rust with Mutex
- load_config IPC command works from React frontend
- All config tests pass
- Builds on macOS arm64
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
