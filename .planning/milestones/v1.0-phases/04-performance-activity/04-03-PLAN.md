---
phase: 04-performance-activity
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: []
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, WEB-02, WEB-03, WEB-04, VIS-03]

must_haves:
  truths:
    - "App cold-starts in under 1 second (no webviews created at startup)"
    - "Switching between two cached apps feels instant (under 100ms perceived)"
    - "With 10 apps visited, RAM stays under 500 MB (LRU evicts at pool cap 8)"
    - "Background app title change produces a white dot badge on its sidebar icon"
    - "Badge clears when user switches to that app"
---

<objective>
Verify the full Phase 4 performance contract and activity badge functionality with a human tester.

Purpose: PERF-01, PERF-02, PERF-03 cannot be unit tested — they require real macOS WKWebView processes, Activity Monitor RAM readings, and perceived latency measurement. The activity badge requires a real third-party site (Gmail/Linear) to validate title change detection through the MutationObserver.

Output: Human-verified confirmation that all Phase 4 success criteria pass.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-performance-activity/04-01-SUMMARY.md
@.planning/phases/04-performance-activity/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of Phase 4 performance contract and activity badges</name>
  <files></files>
  <action>
  Run `cd /Users/angelbotto/Dev/hacks/nexus && cargo tauri dev` and have the user test the following:

  **1. Cold startup (PERF-01, WEB-02):**
  - Quit Nexus completely, then launch via `cargo tauri dev`
  - Time from launch to sidebar visible — should be under 1 second
  - Confirm NO webviews load automatically (the content area should be empty until you click an app)

  **2. Instant switching (PERF-02, WEB-04):**
  - Click on App A (e.g., Gmail) — it loads for the first time
  - Click on App B (e.g., Linear) — it loads for the first time
  - Click back on App A — it should appear INSTANTLY (no reload, no white flash)
  - Switching between two cached apps should feel under 100ms

  **3. LRU eviction (WEB-03, PERF-03):**
  - Configure 10+ apps in ~/.nexus/apps.json
  - Click through 9 different apps one by one
  - Open Activity Monitor and check the total memory of all Nexus-related processes
  - Should be under 500 MB
  - Go back to the 1st app you visited — it should reload (it was evicted from the LRU pool of 8)
  - The 2nd-9th apps should still be cached (no reload when switching to them)

  **4. Activity badge (VIS-03):**
  - Have Gmail (or any app that updates its page title) configured
  - Switch to a different app so Gmail is in the background
  - Wait for a new email or trigger a title change (send yourself an email)
  - Check if a small white dot appears next to Gmail in the sidebar
  - Click on Gmail — the dot should disappear immediately
  - Verify: the active app NEVER shows a dot badge

  **5. Keyboard shortcut badge clearing:**
  - Get a badge to appear on an app
  - Use Cmd+N (N=position) to switch to that app via keyboard shortcut
  - Confirm the badge clears (not just on click, also on keyboard switch)
  </action>
  <verify>User confirms all 5 verification sections pass by typing "approved"</verify>
  <done>All Phase 4 success criteria verified: sub-1s startup, instant switching, LRU eviction under 500MB, badge appears on title change, badge clears on switch</done>
</task>

</tasks>

<verification>
All Phase 4 success criteria from ROADMAP.md verified by human tester:
1. Cold start under 1 second
2. Instant switching between cached apps
3. LRU eviction at pool cap, RAM under 500 MB
4. Activity badge appears on background title change and clears on switch
</verification>

<success_criteria>
Human confirms all 5 verification sections pass. Any failures feed back into gap closure plans.
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance-activity/04-03-SUMMARY.md`
</output>
