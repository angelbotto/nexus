---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/hooks/useAppsConfig.ts
  - src-tauri/src/commands/config.rs
  - src-tauri/src/lib.rs
autonomous: false
requirements:
  - WEB-07
  - CONF-04

must_haves:
  truths:
    - "Clicking an external link (different domain, non-OAuth) in an app webview opens it in the system browser"
    - "OAuth flows (accounts.google.com, login.microsoftonline.com) stay inside the webview"
    - "Subdomain navigations stay inside the webview"
    - "Popups (window.open) to external domains open in system browser"
    - "Editing ~/.nexus/apps.json externally causes the sidebar to update without restarting"
    - "File watcher is debounced (no flicker on editor atomic saves)"
  artifacts:
    - path: "src/hooks/useAppsConfig.ts"
      provides: "File watcher integration that re-fetches config on change"
      contains: "watch"
    - path: "src-tauri/src/commands/config.rs"
      provides: "reload_config command that re-reads file and updates state"
      contains: "reload_config"
  key_links:
    - from: "src/hooks/useAppsConfig.ts"
      to: "@tauri-apps/plugin-fs"
      via: "watch() on ~/.nexus/apps.json with 300ms debounce"
      pattern: "watch.*apps\\.json"
    - from: "src/hooks/useAppsConfig.ts"
      to: "src-tauri/src/commands/config.rs"
      via: "invoke('reload_config') on watch event then invoke('load_config')"
      pattern: "invoke.*reload_config"
---

<objective>
Add config file watching for hot-reload and verify external link routing works end-to-end.

Purpose: Complete the remaining Phase 1 requirements -- CONF-04 (file watcher) and WEB-07 (external links). The external link routing logic (on_navigation/on_new_window) was wired in Plan 02's webview creation; this plan adds the file watcher and does a full human verification of all 5 success criteria.

Output: Complete Phase 1 with all success criteria verifiable.
</objective>

<execution_context>
@/Users/angelbotto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/angelbotto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config file watcher and reload command</name>
  <files>
    src-tauri/src/commands/config.rs,
    src-tauri/src/lib.rs,
    src/hooks/useAppsConfig.ts
  </files>
  <action>
    1. Add `reload_config` command to `src-tauri/src/commands/config.rs`:
       - `#[tauri::command] pub fn reload_config(state: State<'_, Mutex<AppState>>) -> Result<NexusConfig, String>`
       - Re-reads ~/.nexus/apps.json from disk using `config::load_or_create_config()`
       - Updates `state.config` with new config
       - Returns the new config
       - If JSON is invalid, keeps existing config and returns error (do NOT replace good config with default on corrupt reload)

    2. Register `commands::config::reload_config` in `generate_handler!` in `lib.rs`.

    3. Update `src/hooks/useAppsConfig.ts` to add file watching:
       - Import `watch` from `@tauri-apps/plugin-fs` and `homeDir` from `@tauri-apps/api/path`
       - In useEffect: after initial load_config, set up watcher on `${home}/.nexus/apps.json` with `delayMs: 300` (debounce per RESEARCH.md Pitfall 5)
       - On watch event: call `invoke('reload_config')` to re-read from disk, update React state with new config
       - Clean up watcher on unmount (watch returns an unwatch function)
       - Handle errors gracefully (if reload fails, keep showing current config)

    Per RESEARCH.md:
    - Use `watch()` with delayMs: 300, NOT `watchImmediate()` (avoids partial-write issues)
    - Frontend-owned watch approach (Option A from research): watch triggers re-invoke of load_config
    - Must handle atomic saves (editors write temp file then rename)
  </action>
  <verify>
    <automated>cd /Users/angelbotto/Dev/hacks/nexus && pnpm tsc --noEmit 2>&1 | tail -10 && cd src-tauri && cargo test 2>&1 | tail -10</automated>
  </verify>
  <done>
    - reload_config command re-reads config from disk and updates state
    - File watcher set up with 300ms debounce on ~/.nexus/apps.json
    - Sidebar updates when apps.json is edited externally
    - TypeScript and Rust compile without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 1 success criteria</name>
  <files></files>
  <action>
    Run the app with `pnpm tauri dev` and verify all 5 Phase 1 success criteria manually. This is the phase gate -- no code changes, only verification.
  </action>
  <what-built>
    Complete Phase 1 foundation: Tauri 2 app with config layer, session-isolated WebviewWindows, external link routing, and config hot-reload. All 5 Phase 1 success criteria should be verifiable.
  </what-built>
  <how-to-verify>
    Run the app: `pnpm tauri dev`

    **Criterion 1 - Sidebar from config:**
    1. App launches and displays a sidebar with apps from ~/.nexus/apps.json
    2. You should see Plane, Linear, Gmail, GitHub (auto-created on first run)

    **Criterion 2 - Session isolation:**
    1. Click "Gmail" in sidebar -- webview should load mail.google.com
    2. Log into Gmail
    3. Click "Linear" in sidebar -- webview should load linear.app
    4. Verify you are NOT logged into Linear with your Google account (sessions isolated)

    **Criterion 3 - External links:**
    1. Inside any app webview, click a link that goes to a different domain
    2. Verify it opens in your system browser (Safari/Chrome), not inside Nexus
    3. Verify OAuth flows (Google login) still work inside the webview

    **Criterion 4 - Session persistence:**
    1. While logged into Gmail in the webview, quit Nexus (Cmd+Q)
    2. Relaunch with `pnpm tauri dev`
    3. Click Gmail again -- verify you're still logged in

    **Criterion 5 - Hot reload:**
    1. Open ~/.nexus/apps.json in a text editor
    2. Add a new app entry: `{ "id": "notion", "name": "Notion", "url": "https://notion.so", "group": "tools" }`
    3. Save the file
    4. Verify the sidebar updates within 1 second to show Notion (no restart needed)
  </how-to-verify>
  <verify>
    <automated>cd /Users/angelbotto/Dev/hacks/nexus && cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 Phase 1 success criteria pass human verification</done>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe which criteria failed and what happened</resume-signal>
</task>

</tasks>

<verification>
- `cargo test -p nexus` -- all tests pass
- `pnpm tsc --noEmit` -- TypeScript compiles
- All 5 Phase 1 success criteria verified by human
</verification>

<success_criteria>
- External links open in system browser, OAuth stays in webview
- Config hot-reload works within 1 second of file save
- All 5 Phase 1 success criteria pass human verification
- Phase 1 is complete and ready for Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
